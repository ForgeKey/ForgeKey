/**
 * Router module exports
 * Centralized routing system using Zustand
 */

export { useRouter } from './store';
export { ROUTES, ROUTE_CONFIG } from './types';
export type { Route, RouteName, RouteParams, RouteConfig } from './types';
